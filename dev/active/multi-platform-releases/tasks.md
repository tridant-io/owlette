# multi-platform releases — Tasks
**Progress**: 15/15 complete

Read `plan.md` first. Platform keys are `windows_x64`, `macos_arm64`, `linux_x64` (`linux_arm64` reserved, not built). The agent's values are `osFamily ∈ {windows, macos, linux}` and `arch ∈ {x64, arm64}`; a missing `osFamily` means a pre-3.4 Windows agent. All user-facing copy lowercase. Lint every web file you touch (`npx eslint <file>`), run the tests you touch, add the entry under `## [Unreleased]` in BOTH `docs/changelog.md` and `web/content/docs/changelog.mdx` in the task that ships the user-visible change.

## Wave 1: Foundations

- [x] **Task 1.1: the shared platform module**
  - Files: `web/lib/installerPlatform.ts` (new), `web/__tests__/lib/installerPlatform.test.ts` (new)
  - Do: a pure module with no Firestore or React imports, usable from route handlers, hooks and the CLI-facing builder. Export: `INSTALLER_PLATFORMS = ['windows_x64','macos_arm64','linux_x64'] as const` and the `InstallerPlatform` type; `PLATFORM_LABEL` (`windows` / `macos (apple silicon)` / `linux (.deb)`); `PLATFORM_EXT` (`exe`/`pkg`/`deb`) and `platformFromExtension(fileName)` (case-insensitive, null for anything else); `installerFileName(version, platform)` → `Owlette-Installer-v{version}.{ext}`; `platformFromUserAgent(ua: string | null)` → `macos_arm64` for `Macintosh`/`Mac OS X`, `linux_x64` for `Linux` or `X11` that is not Android, otherwise `windows_x64` (curl, bots, unknown → windows); `platformFromOsParam(os: string | null)` → `windows|macos|linux` to key, null otherwise; `detectBrowserPlatform(nav: Navigator)` → `{ platform, intelMac: boolean }` using `navigator.userAgentData.platform` when present and `getHighEntropyValues(['architecture'])` when available (async), falling back to `navigator.platform`/UA — `intelMac` true only when a macOS platform reports architecture `x86`; `InstallerFile` type `{ download_url: string; checksum_sha256: string | null; file_size: number | null; file_name: string | null; uploaded_at: number | null }`; `normalizeInstallerFiles(record: Record<string, unknown>)` → `Partial<Record<InstallerPlatform, InstallerFile>>`: reads `record.files` (each entry validated field by field; unknown keys dropped) and, when `files` is absent or lacks `windows_x64` but the top-level `download_url` is a string, synthesises `windows_x64` from the flat fields (`file_name` = `installerFileName(record.version, 'windows_x64')` when version is a string). Comments lowercase, only for the non-obvious why.
  - Done when: tests cover every UA branch (Windows Chrome, Mac Safari, Mac Chrome, Linux Firefox, Android Chrome → windows, curl → windows, null → windows), the `?os=` mapping, extension mapping incl. uppercase, a legacy record (flat fields only) normalising to `windows_x64`, a record with both (files wins), and a malformed `files` entry being dropped. `npx eslint` clean.

- [x] **Task 1.2: one Linux `.deb`**
  - Files: `agent/build/linux/build.sh`, `agent/packaging/linux/debian/postinst`, `agent/packaging/linux/debian/prerm` (only if the merged layout needs it), `docs/changelog.md`, `web/content/docs/changelog.mdx`
  - Do: after `npx tauri build --bundles deb --ci`, unpack the Tauri deb into the payload (`dpkg-deb -R <app.deb> <tmp>`; copy its `usr/` tree — `/usr/bin/owlette-desktop`, the `.desktop` file, icons — into `work/payload/`; read its `control` `Depends:`). Generate ONE control file: `Package: owlette-agent`, the Tauri Depends merged with `polkitd | policykit-1` (drop `owlette (>= …)`), plus `Provides: owlette`, `Replaces: owlette`, `Conflicts: owlette` so a box with the old two-package install upgrades cleanly. Recompute `Installed-Size` after the merge. Output exactly one file: `agent/build/linux/Owlette-Installer-v${VERSION}.deb` (the same basename convention as the exe and pkg; apt does not care about the filename of a local path). Pin the python-build-standalone tarball: add `PBS_SHA256_x86_64` / `PBS_SHA256_aarch64` constants next to `PYTHON_BUILD` and verify with `sha256sum -c` after download (get the values from the GitHub release's `SHA256SUMS`). Keep `--skip-app`. Update the header usage comment. Verify on the kiosk VM (`reference_kiosk_vm.md` in memory: `ssh -i C:/VMs/owlette-kiosk/ssh/id_ed25519 kiosk@172.22.80.231`, passwordless sudo, remote `kiosk` for the repo): build there, `sudo apt-get install ./Owlette-Installer-v<ver>.deb` over the current two-package install, confirm `dpkg -l | grep owlette` shows one package, the service is active and the desktop unit starts; then prove the agent's own path: stage the deb as `<data_root>/cache/update/owlette-update.deb` and run `apt-get install --simulate` as the agent does (`agent/src/installer_utils.py:690-727`) — it must pass with no unsatisfiable dependency.
  - Done when: one `.deb` per build, the kiosk upgrade from the two-package install succeeds, the simulate check passes, the service and desktop come back on the new version, changelog entry added.

- [x] **Task 1.3: macOS notarization by API key**
  - Files: `agent/build/macos/build.sh`
  - Do: add `--notary-key <path.p8> --notary-key-id <id> --notary-issuer <uuid>` as an alternative to `--notarize <keychain profile>`; when the three are given, `xcrun notarytool submit … --key … --key-id … --issuer … --wait` (and the same for `notarytool log` on failure if the script prints it). Exactly one of the two forms may be given; refuse both. Pin the python-build-standalone tarball sha256 the same way as task 1.2 (`PBS_SHA256` constant, `shasum -a 256 -c`). Keep the header usage comment current. Do NOT rebuild the desktop app on the MBA unless `--skip-app` cannot be used (the owner's running app holds a TCC grant).
  - Done when: `bash -n` passes, the MBA LaunchAgent build (`scratchpad/spike-tcc/app.owlette.build.plist`, `--skip-app --installer-identity … --notarize owlette-notary`, log `~/src/signed-build.log`) still ends `status: Accepted` and `BUILD-EXIT=0`, and a run with a wrong sha256 constant fails before pip.

## Wave 2: API

- [x] **Task 2.1: upload one file per platform**
  - Files: `web/app/api/installer/upload/route.ts`, `web/__tests__/api/installer-public.test.ts`
  - Depends on: Task 1.1
  - Do: POST — accept optional `platform` (must be in `INSTALLER_PLATFORMS`, 400 otherwise); when absent derive it with `platformFromExtension(body.fileName)`; `fileName` must end in `.exe`, `.pkg` or `.deb` (400 message names the three); when both are given the extension must match the platform. Storage path `agent-installers/versions/${version}/${installerFileName(version, platform)}`. Persist `platform` on the `installer_uploads/{uploadId}` doc; audit attributes gain `platform`. PUT finalize — replace the `.set()` with a transaction on `installer_metadata/data/versions/{version}`: if the doc is missing (or `deletedAt` is a number — a re-upload of a deleted version revives it with fresh version-level fields) write `version, release_notes, uploaded_at, release_date (Timestamp), uploaded_by, deletedAt: null`; always merge `files.<platform> = { download_url, checksum_sha256, file_size, file_name: uploadData.fileName, uploaded_at: now }`; when platform is `windows_x64` also set the top-level `download_url`, `checksum_sha256`, `file_size`. When `setAsLatest`, write `latest` from the merged doc (all fields, `release_date` as ISO string, `promoted_at: now`, `promoted_by: userId`). Response adds `platform` and `files`. Keep every existing response field. Do not touch the unused `web/lib/actions/uploadInstaller.server.ts` beyond what compiles.
  - Done when: existing tests pass unchanged; new tests prove: a `.pkg` under an existing version leaves `windows_x64` intact and does not touch the alias; a `.deb` with `platform: 'macos_arm64'` is refused; an unknown extension is refused with the three named; a re-upload of `windows_x64` refreshes the alias; `latest` after a mac upload with `setAsLatest` carries both entries.

- [x] **Task 2.2: the record reads back with `files`**
  - Files: `web/lib/installerVersionResponse.server.ts`, `web/app/api/installer/[version]/set-latest/route.ts`, `web/openapi.yaml`, `web/__tests__/api/installer-files.test.ts` (new), `web/__tests__/api/openapi.test.ts` (only if a schema assertion needs it)
  - Depends on: Task 1.1
  - Do: `InstallerVersionResponse` gains `files: Partial<Record<InstallerPlatform, InstallerFile>>` built with `normalizeInstallerFiles(data)` (so a legacy doc reads back with `windows_x64`); set-latest writes the builder output, which now carries `files`, so nothing is lost on promote/rollback. OpenAPI: new `InstallerFile` schema, `files` (object, additionalProperties `InstallerFile`, keys documented) on `InstallerVersion`, `platform` on the upload request body (enum) and response, the `fileName` description names the three extensions. `download_url` stays `required` (it is the Windows alias).
  - Done when: `GET /api/installer`, `GET /api/installer/latest` and `POST …/set-latest` all return `files`; a legacy doc returns `files.windows_x64` synthesised from the flat fields; the OpenAPI test suite passes; `npm run lint` clean on touched files.

- [x] **Task 2.3: `/download` picks a platform**
  - Files: `web/app/download/route.ts`, `web/e2e/specs/public/static.spec.ts`, `web/e2e/helpers/coverageSeed.ts` (seed a `files` map on the latest version)
  - Depends on: Task 1.1
  - Do: read `?os=` with `platformFromOsParam`; otherwise `platformFromUserAgent(request.headers.get('user-agent'))`; resolve `normalizeInstallerFiles(versionDoc)[platform]?.download_url` (fall back to the latest doc the way the route does today); redirect to it; when the version has no file for that platform respond 404 with a plain-text problem (`no linux build in v3.4.0`) rather than redirecting to `/login`; keep the `/login` redirect for "no latest at all". Add `Vary: User-Agent` on the redirect. Set `Cache-Control: no-store`.
  - Done when: e2e covers the three `?os=` values, a Mac UA with no param, curl with no param (→ exe), a platform with no file (404), and the unchanged `/login` fallback.

- [x] **Task 2.4: the n-file upload script**
  - Files: `scripts/upload-installer.mjs` (new), `.claude/skills/build-system.md` ("Agent Installer Release" steps 4–5), `docs/runbooks/agent-installer-release.md` (the same steps)
  - Depends on: Task 2.1
  - Do: `node scripts/upload-installer.mjs --env dev|prod --version X.Y.Z [--notes "…"] [--set-latest] <file>…` — for each file: sha256 locally, POST `/api/installer/upload` with `{version, fileName: basename, platform: platformFromExtension, releaseNotes, setAsLatest}` (setAsLatest only on the LAST file so the latest pointer carries every entry), PUT the bytes to the signed URL (stream, do not buffer), PUT finalize with `{uploadId, checksum_sha256}`; one `Idempotency-Key` per file per step (`installer-<step>-<version>-<platform>`); reads `OWLETTE_API_KEY` / `OWLETTE_API_KEY_PROD` and the base URLs from `.claude/.env.local` like `scripts/upload-cortex-cli.mjs` does; prints the finalize response per file and the resulting `GET /api/installer/latest` `files` keys. Never prints a key. Replace the curl recipe in the skill and the runbook with the script invocation and keep the manual curl form as a fallback note.
  - Done when: a dry run against dev with the three current artifacts (exe from `agent/build/installer_output/`, pkg from the MBA `~/Desktop`, deb from task 1.2) lands one version doc with three `files` entries and `latest` carrying all three. Needs the owner's go before the dev upload (it changes what the dev fleet's "update" offers).

## Wave 3: Consumers

- [x] **Task 3.1: the download button knows the platform**
  - Files: `web/hooks/useInstallerVersion.ts`, `web/components/DownloadButton.tsx`, `web/app/dashboard/page.tsx` (getting-started card, ~L1226-1275), `web/__tests__/components/DownloadButton.test.tsx` (new)
  - Depends on: Task 1.1, Task 2.2
  - Do: the hook returns `files` (via `normalizeInstallerFiles(data)`) and keeps `downloadUrl` (= `files.windows_x64?.download_url ?? null`) for callers that only want Windows. `DownloadButton`: on mount run `detectBrowserPlatform(navigator)`; primary action downloads the detected platform's file when present (label `download v3.4.0 for macos`), a `DropdownMenu` (existing shadcn primitive) lists every platform in `INSTALLER_PLATFORMS` with `PLATFORM_LABEL`, disabled with tooltip `no linux build in this version` when absent; macOS item reads `macos (apple silicon)` and is disabled with tooltip `intel macs are not supported` when `intelMac`; copy-link copies the primary's URL. The getting-started card uses `DownloadButton` instead of its own `downloadUrl` buttons. Lowercase copy.
  - Done when: the test renders the button under a mocked Windows, Mac and Intel-Mac navigator and asserts the primary label, the menu items and the disabled states; `npx eslint` clean; the dashboard pages that mount `DownloadButton` still build.

- [x] **Task 3.2: the fleet update sends each machine its own file**
  - Files: `web/lib/firebase.ts` (`getLatestOwletteVersion`, `sendOwletteUpdateCommand`), `web/hooks/useOwletteUpdates.ts`, `web/components/UpdateOwletteButton.tsx`, `web/__tests__/hooks/useOwletteUpdates.test.tsx` (new), `docs/changelog.md`, `web/content/docs/changelog.mdx`
  - Depends on: Task 1.1, Task 2.2
  - Do: `getLatestOwletteVersion` returns `files` (normalised) alongside the existing fields. `sendOwletteUpdateCommand(siteId, machineId, installerUrl, deploymentId, targetVersion, checksum)` sends the URL and checksum it is given — delete the block that re-reads `latest.download_url` (`firebase.ts:211-221`). `useOwletteUpdates.updateMachines`: for each machine compute `key = `${machine.osFamily ?? 'windows'}_${machine.arch ?? 'x64'}``; when `files[key]` exists send that URL/checksum; otherwise skip and collect `{ machineId, reason: 'no <label> build in v<version>' }`; return the skipped list; `UpdateOwletteButton` shows the skipped machines in its result toast/notice. "Outdated" still means version only.
  - Done when: the test sends a Mac the pkg URL and its checksum, a Windows machine with no `osFamily` the exe, and skips a Linux machine when the version has no `linux_x64`, with the skip reason surfaced; existing `executeMachineCommand` tests untouched; changelog entry added.

- [x] **Task 3.3: admin uploads and lists per platform**
  - Files: `web/components/admin/UploadInstallerDialog.tsx`, `web/hooks/useInstallerManagement.ts`, `web/app/admin/installers/page.tsx`, `web/e2e/specs/admin/installers.spec.ts`
  - Depends on: Task 2.1, Task 2.2
  - Do: the dialog accepts `.exe,.pkg,.deb`, shows the derived platform label next to the chosen file, refuses a file whose extension maps to nothing, and sends `platform` on the POST. The versions table renders one sub-row per `files` entry (platform label, size, sha256 prefix, download link) under each version; a version missing a platform shows `—` for it. `setAsLatest` copy: `set as latest (all platforms)`.
  - Done when: the admin e2e uploads a `.pkg` under an existing version and sees both rows; lint clean.

- [x] **Task 3.4: CLI and SDK types**
  - Files: `cli/src/commands/installer.ts`, `cli/__tests__/commands/installer-http.test.ts`, `sdks/node/src/resources/installer.ts`, `sdks/node/__tests__/resources.test.ts`, `sdks/python/roost/resources/installer.py`, `sdks/python/tests/test_installer.py`
  - Depends on: Task 2.2
  - Do: CLI `installer upload <file> [--platform <key>]` — platform from the extension by default, refuse an unknown extension client-side with the three named; `installer latest` / `list` print one line per `files` entry (platform, size, sha256 prefix) under the version. Node SDK: `InstallerFile` and `files` on `InstallerVersion`, `platform` on the upload request; Python SDK the same (`files: dict[str, InstallerFile]`). No behaviour change for callers that ignore `files`.
  - Done when: the CLI test uploads a `.deb` and asserts `platform: 'linux_x64'` in the POST body; both SDK test suites pass; no new lint errors.

## Wave 4: Surfaces and release

- [x] **Task 4.1: the "+" modal**
  - Files: `web/app/dashboard/components/AddMachineButton.tsx`, `web/e2e/screenshots/add-machine-modal.spec.ts`, `web/e2e/screenshots/getting-started-install.spec.ts`
  - Depends on: Task 3.1
  - Do: the download row uses the same detection and menu as `DownloadButton` (extract a shared `InstallerDownloadMenu` from 3.1 if it keeps the modal small). The silent-install snippet stays Windows-only and unchanged in text (the e2e video regex asserts it); under it one lowercase line: `silent install is windows only; on macos and linux pair from the app after installing`. Lowercase copy.
  - Done when: both screenshot specs pass; `e2e/videos/03-install-and-pair.video.ts` regex untouched and passing.

- [x] **Task 4.2: release workflow — three artifacts, one provenance**
  - Files: `.github/workflows/build-installer.yml`, `scripts/env-manifest.json` (a `github-repo-secrets` documentation section: the eight Apple names, `never-set` style — names only), `docs/internal/slsa-build-l3.md`
  - Depends on: Task 1.2, Task 1.3
  - Do: rename `build` → `build-windows` (unchanged steps; artifact `installer-windows`). Add `build-macos` on `macos-15`: checkout (`persist-credentials: false`), `setup-node` 22 (`package-manager-cache: false`), `dtolnay/rust-toolchain` 1.98.1 (SHA-pinned like `rust-build.yml`), `rust-cache` for `desktop/src-tauri` with `save-if` main only, temp keychain (`security create-keychain`, import both p12 secrets with `-T /usr/bin/codesign -T /usr/bin/productbuild`, `set-key-partition-list -S apple-tool:,apple:,codesign:`, add to the search list), decode `APPLE_API_KEY_BASE64` to `$RUNNER_TEMP`, resolve the two identity names with `security find-identity -v -p codesigning` / `-p basic` by team id, write the resolved version to `agent/VERSION`, run `APPLE_SIGNING_IDENTITY=… agent/build/macos/build.sh --installer-identity … --notary-key … --notary-key-id … --notary-issuer …`, then `xcrun stapler validate`, `spctl -a -vvv -t install`; `if: always()` cleanup deletes the keychain and the .p8; `timeout-minutes: 60`; artifact `installer-macos`. Do NOT export `APPLE_ID`/`APPLE_PASSWORD`/`APPLE_API_*` names Tauri reads, or the app is notarized twice. Add `build-linux` on `ubuntu-24.04`: apt Tauri deps (`libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev patchelf libxdo-dev libssl-dev build-essential`), node 22, rust 1.98.1, `agent/build/linux/build.sh`, artifact `installer-linux`. New `digest` job (ubuntu): `download-artifact` with `pattern: installer-*` and `merge-multiple: true`, `sha256sum Owlette-Installer-v$V.exe Owlette-Installer-v$V.pkg Owlette-Installer-v$V.deb | base64 -w0` → `subjects-b64`, `version`. `provenance` takes `needs.digest.outputs.subjects-b64` (generator unchanged, `@v2.1.0` with its zizmor rationale). `release` downloads all three and lists all three in `files:`. `verify` downloads all three and runs `slsa-verifier verify-artifact <exe> <pkg> <deb> …`. Top-level `concurrency: { group: build-installer-${{ github.ref }}, cancel-in-progress: false }`. Job-level `permissions: contents: read` everywhere but provenance/release. Every new `uses:` SHA-pinned with a version comment; context only via `env:`. The Apple secrets come from `environment: release` (create it on GitHub restricted to `v*` tags; owner action, note it in the PR). Update `slsa-build-l3.md` to the n-subject verify and the current generator tag.
  - Done when: `zizmor` passes on the file; a `workflow_dispatch` run on a scratch branch produces three artifacts (macOS needs the secrets — if they are not present yet, the job must fail fast with a clear message and the other two must still succeed); a tag run passes `verify` on all three. The eight secrets and the `release` environment are the owner's to add; block on them, do not invent values.

- [x] **Task 4.3: docs**
  - Files: `web/content/docs/agent/installation.mdx`, `web/content/docs/agent/self-update.mdx`, `web/content/docs/dashboard/admin/installer-management.mdx`, `web/content/docs/cli/reference/installer.mdx`, `web/content/docs/getting-started.mdx`, `web/content/docs/dashboard/getting-started.mdx`, `docs/runbooks/agent-installer-release.md`, `README.md`
  - Depends on: Task 2.3, Task 2.4, Task 3.4
  - Do: every page that names the exe names the three files and the platform keys; installation gets a per-platform section (Windows unchanged; macOS: Apple silicon, macOS 15+, `sudo installer -pkg … -target /` or double-click, Screen Recording grant; Linux: `sudo apt-get install ./Owlette-Installer-v<ver>.deb`, Ubuntu 24.04); self-update says the fleet update picks the file by the machine's platform; the `/download?os=` override is documented on installation and in the README's download line; the runbook's upload step points at `scripts/upload-installer.mjs`. Lowercase headings per the docs' existing style. Do not create new `docs/*.md` files.
  - Done when: `npm run build` renders the docs without MDX errors; every occurrence of `Owlette-Installer-v…exe` in those pages is either the Windows line of a three-line list or the silent-install command.

## Wave 5: Proof

- [x] **Task 5.1: three files on dev, three platforms updated**
  - Files: none (verification; log the result in this file's Log)
  - Depends on: every task above
  - Do: bump to the next version (`node scripts/sync-versions.js`), changelog entry, build all three (Windows `build_installer_full.bat`, macOS via the MBA LaunchAgent job, Linux on the kiosk VM or CI), upload with `scripts/upload-installer.mjs --env dev --set-latest`, read back `GET /api/installer/latest`, hit `/download` with `?os=` all three ways and with a Mac UA, then update B4A, the MBA and the kiosk from the deployments page. Each fleet push and the dev upload wait for the owner's explicit go. Confirm through `GET /api/sites/default_site/machines/<id>` (`agentVersion`, `online`).
  - Done when: all three machines report the new version and `files` on `latest` has three entries. Record the timings and any refusal in the Log.

## Log
### 2026-09-25
- Plan created from three research passes (installer API and record; consumers; release workflow and builders). Ready for execution.

### 2026-09-25 — wave 1 complete
- Where: branch `feat/multi-platform-releases` in the worktree `C:\Users\admin\Documents\Git-restored\Owlette-mpr-wt` (cut from `dev`, uncommitted; `web/node_modules` installed there). The main checkout was busy on `fix/memory-total` (PR #246), so nothing was switched there.
- 1.1 `web/lib/installerPlatform.ts` + 21 tests: jest, eslint and `tsc --noEmit` clean. Calls made: the synthesised legacy `windows_x64` carries the top-level `uploaded_at`; `platformFromOsParam` reuses `isMachineOsFamily` from `web/lib/machineOs.ts`; with client hints present `userAgentData.platform` decides alone and the UA only without hints; a rejected `getHighEntropyValues` reads as no hint. Extra exports `InstallerFiles` and `BrowserPlatform` (return types).
- 1.2 one `Owlette-Installer-v<ver>.deb`: the Tauri deb is extracted into the payload, its `Depends` merged with `polkitd | policykit-1`, `Provides/Replaces/Conflicts: owlette`, `Installed-Size` after the merge, `PBS_SHA256_x86_64`/`_aarch64` checked with `sha256sum -c` on every run (cache hit included); `--python` removed as dead with the pin (macOS aligned); postinst `restart`s the desktop user unit for live sessions so the app comes back on the new binary; prerm unchanged. Proven on the kiosk as a throwaway 3.3.16 (VERSION set in the VM working copy only, restored after): the agent-style `apt-get install --simulate` from the two-package state exited 0 (`Remv owlette`, `Inst owlette-agent 3.3.16`), the real upgrade went through in one `apt-get install`, `dpkg -l` shows one package, service and desktop unit active with NRestarts=0 and `/proc/<pid>/exe` = `/usr/bin/owlette-desktop`, dev API `agentVersion 3.3.16`, `online true`. **The kiosk now reports 3.3.16, so the wave-5 version must be higher.** Left on the VM: the merged package running, the previous two debs in `~/old-debs/`, the staged `/var/lib/owlette/cache/update/owlette-update.deb`, the copied `build.sh`/`postinst` in `~/src/owlette` (branch `tri-platform/linux-deb`).
- 1.2 found, not fixed (pre-existing): on a hand-run apt upgrade the old tray app's `useServiceHealth` auto-start answers prerm's `systemctl stop` within the same second (the polkit rule allows it), so the new agent starts from a tree still being unpacked before postinst restarts it; the agent's own self-update writes the update marker first and should stand down (unverified). Candidates: `systemctl mask --runtime` in prerm + unmask in postinst, or prerm writing the marker.
- 1.3 `--notary-key`/`--notary-key-id`/`--notary-issuer` as the alternative to `--notarize`; both forms or a partial set exit 2 before anything builds; `PBS_SHA256` checked with `shasum -a 256 -c` on every run. MBA LaunchAgent build (`--skip-app --installer-identity … --notarize owlette-notary`): `status: Accepted`, stapled, `spctl -t install` accepted, `BUILD-EXIT=0`; the wrong-constant run failed at the checksum before pip. The api-key form itself is unexercised until the App Store Connect key exists (4.2). The Mac's working copy carries the new `build.sh` as a local modification.
- Changelogs: the earlier "Linux packages" entry under `[Unreleased]` was rewritten for the one-file layout in both files rather than adding a second entry that contradicts it.
- Owner still owes: the eight Apple secrets and the `release` environment on GitHub (4.2); the pkg install on the MBA with sudo (5.1).

### 2026-09-25 — wave 2 complete (code); the 2.4 dev dry run is deferred
- 2.1 upload route: POST takes an optional `platform` (400 outside the three), derives it from the extension (`.exe/.pkg/.deb`, 400 naming the three otherwise, 400 on a mismatch), stores `agent-installers/versions/<v>/Owlette-Installer-v<v>.<ext>`, persists and audits `platform`. PUT finalize is one `runTransaction`: fresh version-level fields when the doc is missing or soft-deleted, `files.<platform>` merged, the flat alias refreshed only for `windows_x64`, `latest` written from the merged doc (normalised `files`, ISO `release_date`, `promoted_*`) when `setAsLatest`. Revival starts `files` from only the new entry (deletes are soft, storage stays, but a re-upload is a new build of that number). 47/47 in `installer-public.test.ts` (8 new); `__tests__/api` 80 suites green.
- 2.2 builder: `InstallerVersionResponse.files = normalizeInstallerFiles({ ...data, version })` (the id-keyed doc may lack `version`, which names the synthesised legacy `file_name`); set-latest needed no change (it already writes the builder output); both GETs flow through the builder. OpenAPI: `InstallerFile` schema, `files` on `InstallerVersion` (required; keys documented), `platform` on the upload request/response, three extensions on `fileName`; `download_url` stays required as the Windows alias. New `installer-files.test.ts` (6) asserts the written `latest` doc, not just the response.
- 2.3 `/download`: `?os=` via `platformFromOsParam`, else the User-Agent; the file from the version doc's `files`, falling back to `latest`'s; 404 `text/plain` `no <os> build in v<version>` when the version lacks that platform; `/login` kept for no latest / deleted / error; `Vary: User-Agent` + `Cache-Control: no-store`. `seedInstallerLatest(url, files?)`. e2e `static.spec.ts` 8/8 on chromium against the emulators (JDK 21.0.10, firebase-tools 15.30.1).
- 2.4 `scripts/upload-installer.mjs` (Node built-ins only): per file sha256 → POST (`installer-upload-<v>-<platform>`) → streamed PUT with `Content-Length` and no owlette headers → finalize (`installer-finalize-<v>-<platform>`); `setAsLatest` only on the last file; reads the dev/prod key + URL pair from `.claude/.env.local` (environment wins); prints redacted URLs, each finalize response and the `latest` `files` keys; a non-2xx stops the run, exits 1 and names the files not published. Proven against a local stub (three 3 MB files: bodies, keys, byte counts and sha256s asserted from the stub transcript; key never on stdout; unknown extension, missing file, duplicate platform, missing var and a 500 on the second finalize all refuse cleanly). Skill steps 4–5 and the runbook's step 6, detail section and checklist now lead with the script; curl kept as the fallback. **Deferred: the dry run against dev** — the new route is not deployed there until this branch reaches `dev`, and the upload needs the owner's go; it happens as part of 5.1 (same action, same environment).
- The yamon budget guard (week at 80%) refused background fan-out for 2.4, so it was done inline; expect the same for later waves.
- Build check: `tsc --noEmit` clean, eslint clean on every touched web file, `__tests__/api` + the module test green, `node --check` on the script.

### 2026-09-25 — wave 3 complete
- Ran one foreground agent at a time (the yamon budget guard refuses background fan-out at 80% of the week).
- 3.1 `useInstallerVersion` returns `files` (+ `downloadUrl` = the windows alias, `null` while loading). `DownloadButton` detects the platform on mount, primary = the detected platform's file (label `download v<ver> for <os>`), falling back to the first available platform with the reason in the label; header variant keeps the two aria-labels the e2e videos hover and adds a chevron opening a `DropdownMenu` of the three platforms (disabled + tooltip `no <os> build in this version` / `intel macs are not supported`); `variant="card"` replaces the getting-started card's hand-rolled buttons. 5 component tests; `next build` green (needs `SESSION_SECRET` in the env).
- 3.2 `getLatestOwletteVersion` returns `files`; `sendOwletteUpdateCommand` no longer re-reads `latest.download_url`; `updateMachines` picks `files[osFamily_arch]` (missing → `windows_x64`), skips and returns `{ machineId, reason: 'no <label> build in v…' }` otherwise; `UpdateOwletteButton` lists the skips in the result toast, `No updates sent` when everything was skipped. New hook test (Mac → pkg, no-osFamily → exe, linux without a file → skipped). Changelog entry in both files.
- 3.3 dialog accepts `.exe,.pkg,.deb`, shows the platform badge, refuses other extensions, sends `platform`; the admin table renders one sub-row per platform under each version (`—` where absent); copy `set as latest (all platforms)`. Admin e2e 7/7 including a real `.pkg` upload through the dialog. **Route fix found by the e2e**: finalize's read-URL signing had no emulator branch (`getSignedUrl` needs default credentials), so the dialog's upload 500'd under e2e; added the `IS_E2E` media-URL branch mirroring the POST one (`upload/route.ts`).
- 3.4 CLI `installer upload [--platform]` (platform from the extension, unknown refused with the three named, exit 2), `latest`/`list` print one line per `files` entry; Node SDK `InstallerFile`/`InstallerPlatform`/`files`/`platform`; Python SDK `InstallerFile` dataclass, `files`, `platform=`; both re-exported from the package indexes. CLI jest 291 passed, Node SDK 84 passed, Python SDK 83 passed; tsc clean in all three. Pre-existing, not fixed: `cli` `npm run lint` cannot run (`typescript-eslint` is not installed anywhere; CI has no lint step for cli/sdks); ruff/mypy findings in the Python SDK are unchanged from baseline.
- Build check: web `tsc` clean, eslint clean on every touched file, full web jest suite green, changelogs identical.

### 2026-09-25 — wave 4 complete (code); the workflow's dispatch/tag runs wait on the owner
- 4.1 `AddMachineButton` uses `<DownloadButton variant="inline" />` (a third variant instead of an extracted menu: header is white-on-dark, card too wide for the 448px dialog) plus the line `silent install is windows only; on macos and linux pair from the app after installing`; the command text is byte-identical. Screenshot specs 2/2 (both PNGs under `web/public/docs-screens/` regenerated). Video `03-install-and-pair`: b02 expectation updated to the new tooltip (`download v<ver> for windows`, a 3.1 consequence); the annex-site seeding was pre-existing broken (legacy `users.sites[]` only; access needs the member row) and now uses `grantMembership`; **b08 still fails** — the freshly paired machine card never shows the exact text `online` (card copy drift since the spec was written 2026-09-15; CI never runs the video project). Left as is; the b09 command-box regex is untouched and structurally unaffected.
- 4.2 `build-installer.yml`: `security-gate` → `build-windows` / `build-macos` (macos-15, `environment: release`, temp keychain from the two p12 secrets, `.p8` from `APPLE_API_KEY_BASE64`, identities resolved by team id, `build.sh --installer-identity … --notary-key …`, `stapler validate`, `spctl -t install`, `if: always()` cleanup) / `build-linux` (ubuntu-24.04) → `digest` (three `sha256sum` lines, base64) → `provenance` (generator `@v2.1.0`) → `release` (three files) → `verify` (`slsa-verifier` over exe+pkg+deb). Fail-fast step names missing secrets; windows and linux still complete; nothing is attested without all three. zizmor 1.16.3 clean (the old `cache-poisoning` ignore was inert and now sits on `on:`), actionlint clean apart from the pre-existing `vulnerability-alerts` scope. `env-manifest.json` gains `github-repo-secrets` (eight `APPLE_*` names, never values); `slsa-build-l3.md` rewritten for three subjects. Deferred to the owner: the eight secrets, the `release` environment restricted to `v*` tags, the dispatch and tag runs.
- 4.3 docs: installation (three-file list, `/download` + `?os=`, per-platform sections), self-update (pkg/deb path, fleet update by `osFamily_arch`), admin installer management, CLI reference, both getting-started pages, the runbook (intro, path b jobs, smoke, caveats), README quick start. `fumadocs-mdx` + `next build` green (225 pages). Every remaining `.exe` mention is a Windows line of a three-file list, the silent-install command, or a per-file curl variable. Not touched (out of scope, flagged): the README tagline/badge/intro still say Windows-only.
- Build check: `tsc` clean, eslint clean on the touched web files, full web jest suite green.

### 2026-09-26 — wave 5 complete: 4.0.0 then 4.0.1 on dev, three platforms self-updated
- PR #247 merged to `dev` 06:45 UTC (CI: 19 checks green, emulator e2e 409 passed locally and in CI). Version 4.0.0 (owner's ruling: a major, three platforms + swoop), bumped and pushed to `dev` as 6c420555.
- Builds: Windows here (`build_installer_full.bat`, ~10 min), macOS via a `dev`-branch LaunchAgent job on the MBA (`app.owlette.build-dev`, full app rebuild + Developer ID signing + notarization: `status: Accepted`, stapled, `spctl` Notarized Developer ID), Linux on the kiosk (full Tauri rebuild). Hashes matched across machines.
- Upload: `scripts/upload-installer.mjs --env dev --set-latest` landed one 4.0.0 doc with three `files` and `latest` carrying all three. The very first POST returned 200 with an empty body once (the replay under the same idempotency key was fine); the script now retries once. `/download` on dev: `?os=` ×3, a Mac UA, a Linux UA and curl all redirect to the right 4.0.0 file with `Vary: User-Agent` and `no-store`.
- The owner installed the 4.0.0 pkg on the MBA (first real macOS install). Three defects surfaced: (1) macOS **relocated** the app onto a build-tree copy of the bundle (Installer log `Applications/owlette.app relocated to …/work/payload/app/owlette.app`), so `/Applications/owlette.app` was missing and the tray LaunchAgent exited 78 — fixed with a component plist (`BundleIsRelocatable=false`; the 4.0.1 pkg's PackageInfo reads `relocatable="false"`); (2) the packaged daemon starts with a fresh data root, no firebase section, `environment: production` — on this dev box the dev firebase section and environment were merged into its config and the old `~/owlette-data/.tokens.enc` copied in by the owner with sudo (the token key is bound to the hardware UUID, so it decrypts; `TEC-MBA.local` came online from the package); (3) `build_installer_full.bat`'s clean step `rmdir /s /q build` deleted the tracked `agent/build/{linux,macos}/build.sh` — now cleans only its own four sub-dirs (owner OK'd the exact lines). The relocated 4.0.0 install also left root-owned files in the Mac build tree's `work/`; moved aside as `work.rootowned-4.0.0` (owner may delete it).
- 4.0.1 (the three fixes + the changelog) built, uploaded, promoted (122ebf9e). Fleet, one `update_owlette` command each through the API (curl; node fetch gets `could not read request body` on the commands route — follow-up), file picked by `osFamily_arch` from `latest.files`:
  - TEC-B4A (windows): command 07:37:33 → new service heartbeat 07:38:23, 4.0.1.
  - owlette-kiosk (linux, 3.3.16 → 4.0.1): simulate passed, transient `owlette-update.service` ran apt, `[SUCCESS] Self-update completed successfully!` 17 s after the command; one package, both units active, tray on the new binary.
  - TEC-MBA.local (macos, 4.0.0 → 4.0.1): pkg downloaded and verified, `app.owlette.update` launchd job ran `installer` ("The upgrade was successful"), daemon back on 4.0.1, `/Applications/owlette.app` present, tray LaunchAgent running — the first macOS self-update from a package, and the relocation fix proven.
- Left as is: TEC-A4D (dev, windows) still on 3.3.15 — not in the plan's list; the pre-existing `03-install-and-pair` video b08 failure; the CI macOS job (owner's secrets + `release` environment still pending).
- Follow-ups: node fetch vs the commands route body; the tray-app auto-start race on a hand-run apt upgrade (wave 1 log); Linux capture, arm64, the macOS management profile (tri-platform list).
