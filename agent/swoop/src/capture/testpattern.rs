//! Synthetic capture source. Task 8.7 fills it.
//!
//! Selected only through the bundle's test-only `overrides` object and only in
//! a build carrying the `testhooks` cargo feature, so CI can run the pipeline
//! on a runner with no GPU and no desktop.
