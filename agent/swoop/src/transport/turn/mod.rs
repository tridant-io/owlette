//! Host-side TURN client (plan.md D13). Task 7.4 fills it, behind the `turn`
//! cargo feature: str0m has no TURN client, and Cloudflare bills only
//! server to client egress, so holding the allocation here leaves the video
//! direction unbilled.
